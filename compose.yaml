name: home-command-center

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./backend/.env
    ports:
      - "${PORT:-8000}:8000"
    init: true
    restart: unless-stopped
